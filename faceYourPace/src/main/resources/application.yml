server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://fyp-db.cksdoomihsze.us-west-2.rds.amazonaws.com:3306/fyp_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: fyp-rds
    password: fyp12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace​
